<% content_for 'title' do %>Reset password<% end %>

<% if flash.any? or devise_error_messages? %>
  <div class="m-b-2">
    <% flash.each do |key, value| %>
      <% if not value.blank? %>
        <div class="text-danger text-xs-center ">
          <strong><%= value %></strong>
        </div>
      <% end %>
      <% if devise_error_messages? %>
        <strong><%= devise_error_messages! %></strong>
      <% end %>
    <% end %>
  </div>
<% end %>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <div class="inner-addon left-addon">
    <i class="fa fa-at"></i>
    <%= f.email_field :email,
                        :placeholder => 'Email address',
                        :autofocus => true,
                        :class => 'form-control m-b-2' %>
  </div>

  <div class="m-t-2">
    <%= f.submit 'Send reset link', :class => 'btn btn-primary p-x-2' %>
    <%= link_to new_user_session_path, :class => 'btn pull-xs-right' do %>
      <small>Back</small>
    <% end %>
  </div>
<% end %>
