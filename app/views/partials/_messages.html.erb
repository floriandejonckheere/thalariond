<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
    <%= value %>
    <span class="pull-right">
      <a href="javascript:void(0);" data-toggle="dismiss"><i class="fa fa-close"></i></a>
    </span>
  </div>
<% end %>
