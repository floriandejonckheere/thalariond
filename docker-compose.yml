version: '2'

services:
  app:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - public:/app/public
      - /var/run/postgresql/:/var/run/postgresql/
    env_file:
      - thalariond.env
    restart: always

volumes:
  public: {}
